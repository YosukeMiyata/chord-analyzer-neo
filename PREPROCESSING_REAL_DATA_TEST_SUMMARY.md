# 前処理機能の実データテスト結果

## テスト概要

実際の楽曲データ（真夏の果実）を使用して、前処理機能の効果を検証しました。

## テスト環境

- **音声ファイル**: `test_data/audio/真夏の果実.mp3`
- **正解データ**: `test_data/ground_truth/真夏の果実.txt` (125個のコード進行)
- **予測コード数**: 3009個（前処理なし）

## 実装した改善

### 1. 全角文字対応

正解データに含まれる全角シャープ（`♯`）と全角フラット（`♭`）を半角に変換する機能を追加しました。

**変更箇所**: `src/evaluation/preprocessing.py` の `ChordNormalizer.normalize` メソッド

```python
# Step 1.5: Convert full-width characters to half-width
chord = chord.replace('♯', '#').replace('♭', 'b')
```

## テスト結果

### 前処理設定

- **正規化モード**: SLASH（スラッシュ記法）
- **正規化**: 有効
- **集約**: 無効（正解データにタイムスタンプ情報がないため）

### 評価メトリクスの比較

| メトリクス | 前処理なし | 前処理あり | 変化 |
|-----------|-----------|-----------|------|
| ルート音精度 | 17.22% | 15.52% | ↓ 1.69% |
| コード品質精度 | 12.50% | 8.01% | ↓ 4.49% |
| DTW距離 | 0.7060 | 0.6985 | ↑ 0.0075 (改善) |
| 完全一致率 | 0.07% | 0.30% | ↑ 0.23% (約4倍) |
| 予測コード数 | 3009 | 3009 | 変化なし |

### 結果の分析

#### ポジティブな効果

1. **完全一致率の改善**: 0.07%から0.30%に向上（約4倍）
   - 正規化によってコード表記が統一され、完全一致するケースが増加

2. **DTW距離の改善**: 0.7060から0.6985に減少
   - コード列の類似度が向上

#### ネガティブな効果

1. **ルート音精度の低下**: 17.22%から15.52%に減少
   - 正規化による表記変更が原因（例: `AonC#` → `A/C#`）
   - 正解データの表記と異なる形式に変換された可能性

2. **コード品質精度の低下**: 12.50%から8.01%に減少
   - 同様に正規化による表記変更が影響

## 制限事項

### タイムスタンプ情報の欠如

現在の正解データ（`真夏の果実.txt`）には、タイムスタンプ情報が含まれていません。そのため、以下の機能をテストできませんでした:

- **コード集約**: 高解像度の予測コード（3009個）を正解データの解像度（125個）に集約する機能
- **タイムスタンプベースのアライメント**: 時間情報を使用した精密なコード対応付け

### 集約機能の効果

集約機能を有効にした場合（タイムスタンプ情報がない状態）:
- 予測コード数が3009個から125個に減少
- しかし、ほとんどのコードが`CM/B`に集約されてしまう問題が発生
- これは、位置情報（`position`）が連番（0, 1, 2, ...）であり、実際のタイムスタンプではないため

## 推奨事項

### 1. タイムスタンプ付き正解データの作成

集約機能の効果を正しく評価するには、以下の形式の正解データが必要です:

```
0.0: D
2.5: A/C#
5.0: Bm7
7.5: F#m7
...
```

### 2. 正規化モードの調整

正解データの表記形式に合わせて、正規化モードを選択する必要があります:

- **SLASH**: `A/C#` 形式（スラッシュ記法）
- **ON**: `AonC#` 形式（on記法）
- **STANDARD**: 正規化なし

現在の正解データは`AonC#`形式を使用しているため、`NormalizationMode.ON`を使用すると精度が向上する可能性があります。

### 3. 正規化の改善

正規化処理で以下の改善が必要です:

- 正解データの表記形式を自動検出
- 検出された形式に合わせて正規化モードを選択
- または、正解データも同じ正規化処理を適用

## 結論

前処理機能は正しく実装されており、以下が確認できました:

1. ✅ 全角文字（`♯`, `♭`）の半角変換が正常に動作
2. ✅ コード正規化が正常に動作
3. ✅ 完全一致率とDTW距離が改善
4. ⚠️ タイムスタンプ情報がないため、集約機能の効果は未検証
5. ⚠️ 正規化による表記変更が一部のメトリクスに悪影響

実際の効果を完全に評価するには、タイムスタンプ情報を含む正解データでのテストが必要です。

## 生成されたファイル

- `evaluation_report_without_preprocessing.json`
- `evaluation_report_without_preprocessing.md`
- `evaluation_report_with_preprocessing.json`
- `evaluation_report_with_preprocessing.md`
- `examples/test_preprocessing_with_real_data.py` (テストスクリプト)
